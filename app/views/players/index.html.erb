<body id="player_index">

<h1>Players</h1>
<% @players.each do |player| %>
    <ul>
        <li>
            <%= link_to player.name, player_path(player)%>
        </li>
    </ul>
<%end%>

<%= button_to "Create a new Player", new_player_path(@player), method: "get"%>
<br>
<%= link_to "Back to Main Menu", battles_path %>

</body>